package com.example.backend.service;

import org.bouncycastle.jce.provider.BouncyCastleProvider;


import java.security.Security;

public class Test {

      /*  public static void main(String[] args) {
            Security.addProvider(new BouncyCastleProvider());
            // Créez une instance de CipherService avec un GenKeyService (ou utilisez un mock si nécessaire)
            CipherService cipherService = new CipherService(new GenKeyService());

            try {
                // Définissez les paramètres de test pour l'appel de la méthode decrypt
                String chiffrePath = "C:/TPCryptoJava/chiffre.txt";
                String clairPath = "C:/TPCryptoJava/dechiffre.txt";
                String path = "C:/TPCryptoJava/cle.txt";
                String algo = "AES";
                String provider = "BC"; // ou autre fournisseur de chiffrement

                // Appelez la méthode decrypt
                cipherService.decrypt(chiffrePath, clairPath, path, algo, provider);

                // Ici, vous pouvez effectuer des vérifications supplémentaires pour vous assurer que la méthode decrypt a fonctionné correctement.
                // Par exemple, vous pouvez vérifier l'existence du fichier de sortie, ou lire et vérifier le contenu déchiffré.

                System.out.println("Déchiffrement terminé avec succès !");
            } catch (Exception e) {
                System.err.println("Une erreur s'est produite lors du déchiffrement : " + e.getMessage());
                e.printStackTrace();
            }
        }
    }*/




    }
